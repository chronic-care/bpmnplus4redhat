import com.Patient;

rule "PatientHBA1CStatus"
	dialect "mvel"
	ruleflow-group "patientHBA1CStatus"
	when
		pateint : Patient( pateint.latestHBA1C <= pateint.hbA1cRangeHigh && pateint.latestHBA1C >= pateint.hbA1cRangeLow )
	then
		pateint.setHba1cStatus( "OK" );
end
